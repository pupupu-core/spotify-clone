# Deprecated

This API directory is deprecated. Do not add new services here.

Use/add services from/to `core/services`
Use/add stores from/to `core/stores`

For types and interfaces, use `packages/shared/model` instead.

REMOVE this directory with all the files ON REFACTOR
