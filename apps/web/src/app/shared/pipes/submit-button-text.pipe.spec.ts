import { SubmitButtonTextPipe } from './submit-button-text.pipe';

describe('SubmitButtonTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmitButtonTextPipe();

    expect(pipe).toBeTruthy();
  });
});
